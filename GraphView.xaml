<Window x:Class="CST_EMI_Shield_Wizard.GraphView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="GraphView" Height="450" Width="800">
    <Grid>
        <oxy:PlotView Model="{Binding PlotModel}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Button Content="Move to Max" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Click="MoveToMax_Click"/>
    </Grid>
</Window>
